<h1>購物車</h1>

<%= link_to "清空購物車", cart_path, method: :delete, class: "btn btn-danger btn-sm" %>

<%= render "order" %>

<%= link_to "再回去選購", products_path, class: "btn btn-primary" %>
<%= link_to "結帳", checkout_cart_path, class: "btn btn-danger" unless current_cart.empty? %>
